{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "Maui",
  "title": "Add Mantis UI to project",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project to add Mantis UI to",
      "x-prompt": "What's the name of the project you want to add Mantis UI to?"
    },
    "standalone": {
      "type": "boolean",
      "description": "Whether to use standalone components",
      "default": true,
      "x-prompt": "Do you want to use standalone components? (Y/n)"
    },
    "newWorkspace": {
      "type": "boolean",
      "description": "Whether this is a new workspace",
      "default": false
    }
  },
  "required": ["project"]
}

