{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "Maui",
  "title": "Add Mantis UI to existing project",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project to add Mantis UI to",
      "$default": {
        "$source": "projectName"
      },
      "x-prompt": "What project would you like to add Mantis UI to?"
    },
    "standalone": {
      "type": "boolean",
      "description": "Whether the project uses the standalone component approach",
      "default": false
    },
    "x-prompt": "Is this a standalone component project?"
  },
  "required": ["project"]
}
